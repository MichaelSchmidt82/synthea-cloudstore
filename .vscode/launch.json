{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug gs-etl",
            "type": "docker",
            "request": "launch",
            "preLaunchTask": "docker-run: debug",
            "python": {
                "pathMappings": [
                    {
                        "localRoot": "${workspaceFolder}/",
                        "remoteRoot": "/usr/src/app"
                    }
                ],
            },
        }
    ]
}